/**
 * STAGE 1 PLACEHOLDER.
 * TODO(stage 3): port doctor.html's Hero/Chapter 1-3/Profile sections.
 * TODO(stage 5): add `export const metadata` (title/description/canonical).
 * Note: this is the existing "대표원장 스토리" page — distinct from the new
 * generic /doctor/[slug] profile page introduced by the column system.
 */
export default function DoctorStoryPage() {
  return (
    <div>
      <h1>대표원장 스토리</h1>
    </div>
  );
}
