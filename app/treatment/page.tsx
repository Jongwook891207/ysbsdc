/**
 * STAGE 1 PLACEHOLDER.
 * TODO(stage 3): port treatment.html's Key Treatment 01-03 sections +
 * 12-item general treatment grid + modal detail data.
 * TODO(stage 5): add `export const metadata`.
 */
export default function TreatmentPage() {
  return (
    <div>
      <h1>진료 안내</h1>
    </div>
  );
}
