/**
 * STAGE 1 PLACEHOLDER.
 * TODO(stage 3): port mission.html's Hero/Why We Exist/Vision/Our Promise +
 * 3 Core Values/Closing sections (mobile-specific layout included).
 * TODO(stage 5): add `export const metadata`.
 */
export default function MissionPage() {
  return (
    <div>
      <h1>백세미션</h1>
    </div>
  );
}
